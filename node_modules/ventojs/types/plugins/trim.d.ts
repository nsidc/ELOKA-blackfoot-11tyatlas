import type { Token } from "../core/tokenizer.d.ts";
import type { Environment, Plugin } from "../core/environment.d.ts";
export default function (): Plugin;
export declare function trim(_: Environment, tokens: Token[]): void;
